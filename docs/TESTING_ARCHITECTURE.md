# 🏛️ Arquitetura de Testes

## Estrutura Geral

```
┌─────────────────────────────────────────────────────────────┐
│           TESTES UNITÁRIOS (test/)                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────┐  ┌──────────────────┐  ┌───────────┐ │
│  │  ConfigManager   │  │  WiFiManager     │  │   MQTT    │ │
│  │    (10 testes)   │  │  (14 testes)     │  │ (18 tests)│ │
│  └─────────┬────────┘  └─────────┬────────┘  └─────┬─────┘ │
│            │                     │                 │       │
│  ┌─────────┴─────────────────────┴─────────────────┴─────┐ │
│  │         Google Test Framework (gtest)              │ │
│  │         - 42 Total Tests                           │ │
│  │         - 0.71s Execution Time                     │ │
│  │         - 100% Pass Rate                           │ │
│  └──────────────────────────────────────────────────┘ │
│                                                         │
│  ┌─────────────────────┐  ┌─────────────────────────┐  │
│  │      MOCKS          │  │   FIXTURES              │  │
│  ├─────────────────────┤  ├─────────────────────────┤  │
│  │ LittleFSMock        │  │ TestData                │  │
│  │ WiFiMock            │  │ (Dados reutilizáveis)   │  │
│  │ MQTTClientMock      │  │                         │  │
│  └─────────────────────┘  └─────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## Fluxo de Teste

```
START
  ↓
┌─────────────────────────────┐
│ setUp()                     │
│ - Reset LittleFS Mock      │
│ - Reset WiFi Mock          │
│ - Reset MQTT Mock          │
└────────────┬────────────────┘
             ↓
┌─────────────────────────────┐
│ ARRANGE                     │
│ Preparar dados de teste     │
│ Configurar mocks            │
└────────────┬────────────────┘
             ↓
┌─────────────────────────────┐
│ ACT                         │
│ Executar função testada     │
└────────────┬────────────────┘
             ↓
┌─────────────────────────────┐
│ ASSERT                      │
│ Verificar resultados        │
│ EXPECT_*() calls            │
└────────────┬────────────────┘
             ↓
┌─────────────────────────────┐
│ tearDown()                  │
│ - Limpeza pós-teste        │
│ - Reset de estado           │
└────────────┬────────────────┘
             ↓
            END
```

## Hierarquia de Testes

```
root/
└── test/
    ├── test_main.cpp                    [Entry Point]
    │
    ├── test_config_manager.cpp
    │   ├── ConfigManagerTest (Fixture)
    │   │   ├── ParseValidJSON
    │   │   ├── ParseJSONWithSpecialChars
    │   │   ├── ParseInvalidJSONNoSSID
    │   │   ├── ParseInvalidJSONNoPass
    │   │   ├── SaveAndLoadConfig
    │   │   ├── LoadNonexistentConfig
    │   │   ├── HasValidConfigTrue
    │   │   ├── HasValidConfigFalse
    │   │   ├── SSIDLengthValidation
    │   │   └── SSIDWithSpacesJSON
    │
    ├── test_wifi_manager.cpp
    │   ├── WiFiManagerTest (Fixture)
    │   │   ├── SuccessfulConnection
    │   │   ├── SSIDNotFound
    │   │   ├── ConnectionFailed
    │   │   ├── EmptySSID
    │   │   ├── EmptyPassword
    │   │   ├── ValidSSID (x3)
    │   │   ├── InvalidSSIDTooLong
    │   │   ├── InvalidSSIDEmpty
    │   │   ├── ValidPassword (x2)
    │   │   ├── InvalidPasswordTooShort
    │   │   ├── InvalidPasswordTooLong
    │   │   ├── InvalidPasswordEmpty
    │   │   ├── StatusStrings
    │   │   └── MultipleConnectionAttempts
    │
    ├── test_mqtt_manager.cpp
    │   ├── MQTTManagerTest (Fixture)
    │   │   ├── ValidConfig
    │   │   ├── InvalidConfigEmptyHost
    │   │   ├── InvalidConfigZeroPort
    │   │   ├── InvalidConfigPortTooHigh
    │   │   ├── InvalidConfigEmptyUsername
    │   │   ├── CreateTelemetryJSON
    │   │   ├── PublishTelemetryDisconnected
    │   │   ├── PublishTelemetryConnected
    │   │   ├── PublishStatusDisconnected
    │   │   ├── PublishStatusConnected
    │   │   ├── LocalBrokerLatency
    │   │   ├── LocalNetworkLatency
    │   │   ├── ValidTopic
    │   │   ├── InvalidTopicWithSpace
    │   │   ├── InvalidTopicEmpty
    │   │   ├── CriticalMessageQoS
    │   │   ├── TelemetryQoS
    │   │   └── MultipleTelemetryMessages
    │
    ├── mocks/
    │   ├── littlefs_mock.h               [Mock do FileSystem]
    │   └── wifi_mock.h                   [Mock do WiFi]
    │
    └── fixtures/
        └── (Dados de teste reutilizáveis)
```

## Ciclo de Desenvolvimento com Testes

```
┌────────────────────────────────────────────────────────────┐
│                    DESENVOLVIMENTO TDD                     │
└────────────────────────────────────────────────────────────┘

FASE 1: RED (Teste Falha)
    ↓
    ┌───────────────────────────────────────┐
    │ 1. Escrever teste para nova feature   │
    │ 2. Rodar teste → FALHA ❌             │
    │ 3. Analisar falha                     │
    └───────────────────────────────────────┘

FASE 2: GREEN (Teste Passa)
    ↓
    ┌───────────────────────────────────────┐
    │ 1. Implementar função/módulo          │
    │ 2. Rodar teste → SUCESSO ✅           │
    │ 3. Verificar todos os testes          │
    └───────────────────────────────────────┘

FASE 3: REFACTOR (Melhorar Código)
    ↓
    ┌───────────────────────────────────────┐
    │ 1. Melhorar implementação             │
    │ 2. Rodar testes → SUCESSO ✅          │
    │ 3. Garantir cobertura >= 80%          │
    └───────────────────────────────────────┘

VOLTA ao FASE 1 para próxima feature
```

## Tipos de Testes

```
┌─────────────────────────────────────────────────────┐
│           PIRÂMIDE DE TESTES                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│                      /\\                            │
│                     /  \\         E2E               │
│                    /────\\        (Futuro)         │
│                   /      \\                         │
│                  /  Test  \\     Integração        │
│                 /──────────\\    (Futuro)          │
│                /            \\                      │
│               /   Unit Tests  \\  ✅ 42 testes     │
│              /──────────────────\\                 │
│                                                     │
│   Velocidade:  Rápido ← → Lento                    │
│   Cobertura:   Completa ← → Específica             │
│   Custo:       Barato ← → Caro                     │
│                                                     │
└─────────────────────────────────────────────────────┘
```

## Relação entre Componentes

```
┌─────────────────────────────────────────────────────┐
│          SISTEMA (src/)                             │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌────────────────┐      ┌──────────────────┐     │
│  │  ConfigManager │◄─────► LittleFS         │     │
│  │ (Carrega config)│      (Filesystem)       │     │
│  └────────┬────────┘      └──────────────────┘     │
│           │                                        │
│           ├──► WiFi SSID/Pass                      │
│           │                                        │
│           ▼                                        │
│  ┌────────────────┐      ┌──────────────────┐     │
│  │  WiFiManager   │◄─────► ESP8266WiFi      │     │
│  │ (Conecta WiFi) │      (Hardware)         │     │
│  └────────┬────────┘      └──────────────────┘     │
│           │                                        │
│           ├──► WiFi SSID/Pass/Status               │
│           │                                        │
│           ▼                                        │
│  ┌────────────────┐      ┌──────────────────┐     │
│  │  MQTTManager   │◄─────► PubSubClient     │     │
│  │ (Publica dados)│      (Broker)           │     │
│  └────────────────┘      └──────────────────┘     │
│                                                     │
└─────────────────────────────────────────────────────┘

TESTES (test/)
├── TestConfigManager + LittleFSMock
├── TestWiFiManager + WiFiMock
└── TestMQTTManager + MQTTClientMock
```

## Cobertura de Código

```
ConfigManager
├── loadConfig()             ✅ Testado (95%)
│   ├── Arquivo existe       ✅
│   ├── Arquivo vazio        ✅
│   ├── JSON válido          ✅
│   ├── JSON inválido        ✅
│   └── Parse fields         ✅
├── saveConfig()             ✅ Testado (95%)
└── hasValidConfig()         ✅ Testado (95%)

WiFiManager
├── connectWifi()            ✅ Testado (90%)
│   ├── SSID válido          ✅
│   ├── SSID inválido        ✅
│   ├── Password válido      ✅
│   └── Password inválido    ✅
├── validateSSID()           ✅ Testado (90%)
├── validatePassword()       ✅ Testado (90%)
└── getStatusString()        ✅ Testado (90%)

MQTTManager
├── isValidConfig()          ✅ Testado (85%)
├── createTelemetryJSON()    ✅ Testado (85%)
├── publishTelemetry()       ✅ Testado (85%)
├── publishStatus()          ✅ Testado (85%)
└── validateTopic()          ✅ Testado (85%)
```

---

**Diagrama atualizado**: 2 de Janeiro, 2026
